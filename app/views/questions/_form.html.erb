<%= form_with model: [@question.test, @question], local: true do |form| %>
  
  <%= render 'shared/errors', model_instance: @question %>
  
  <%= form.label :body %>
  <%= form.text_field :body, id: :question_body %>

  <%= form.submit %>
<% end %>